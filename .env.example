# 复制为 .env 可覆盖 docker-compose 中的默认值（仅本地演示，生产务必修改）
# cp .env.example .env

# InfluxDB 组织 / 桶 / 管理 Token（三处需一致：influxdb 初始化、collector、grafana 数据源）
INFLUX_ORG=acme
INFLUX_BUCKET=field
INFLUX_TOKEN=field-demo-token-0123456789
