# Grafana 自动加载 /var/lib/grafana/dashboards 下的看板 JSON
apiVersion: 1

providers:
  - name: field-devices
    orgId: 1
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 15
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
